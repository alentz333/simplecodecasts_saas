<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <div class="well">
            <h1 class="text-center">Questions for the instructor?</h1>
            <p class="text-center">Please clearly state the question or problem you are running into. Make sure to include a working email address.</p>
            <%= form_for @contact do |f| %>
                <div class="form-group">
                    <%= f.label :name %>
                    <%= f.text_field :name, class: 'form-control' %>
                </div>
                <div class="form-group">
                    <%= f.label :email %>
                    <%= f.email_field :email, class: 'form-control' %>
                </div>
                <div class="form-group">
                    <%= f.label :comments %>
                    <%= f.text_area :comments, class: 'form-control' %>
                </div>
                <%= f.submit 'Submit', class: "btn btn-default" %>
            <% end %>
        </div>
    </div>
</div>